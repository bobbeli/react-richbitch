import {userConstants} from "../_constants/userConstants";

export default function reducer(state = {
    points: [
        {
            id: null,
            value: null,
            createdAt: null
        }
    ],
}, action) {
    switch (action.type) {

    }
    return state;
}